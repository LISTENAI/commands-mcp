The current working directory has the following command templates available.
You can get any of these commands by using the `get_command` tool.

{{#each commands}}
## {{{this.name}}}

{{{this.description}}}

{{#if this.args}}
Arguments:

{{#each this.args}}
- **{{{this.name}}}** (
  {{~{this.type}~}}
  {{~#if this.required~}}
    , required
  {{~/if~}}
  ): {{{this.description}}}
{{/each}}
{{else}}
No arguments required.
{{/if}}

{{/each}}
